void main(List<String> args) {
  // Optional Variables / Nullable Variables are variables that can be null or have a value of a specific data type 
  
  //int someValue = null; // will throw an error because null is not a value of int data type
  // so to avoid this error we can use the "?" operator
  int? someValue; // this is a nullable variable
  print(someValue);
}